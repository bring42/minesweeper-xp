# .gitignore

# Large binaries — never commit these directly; they go via GitHub Releases
disk.img
*.img.bak
autorun.img

# v86 files are downloaded by scripts/prepare.sh
public/v86.js
public/v86.wasm
public/seabios.bin
public/vgabios.bin

# Disk chunks are generated by scripts/chunk.py
public/chunks/

# Extracted exe (re-extractable from minesweeperxp/minesweeper.zip)
winmine.exe

# macOS
.DS_Store
.AppleDouble

# Node (not used but just in case)
node_modules/
